<% title("#{User.model_name.human}") %>
<h2><%= "#{User.model_name.human}" %></h2>

<ul>
  <% @users.each do |user| %>
  <li><%= link_to user, [:admin, user] %></li>
  <% end %>
</ul>

<%= link_to t("admin_user.new_title"), new_admin_user_path %>
